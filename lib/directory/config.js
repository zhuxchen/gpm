/*
 *  这里配置请求后台接口
 */
var modelHost = 'localhost';
var modelPort = '8080';
var modelMethod = 'POST';

var config = {
	'modelHost': modelHost,
	'modelPort': modelPort,
	'modelMethod': modelMethod
};

module.exports = config;